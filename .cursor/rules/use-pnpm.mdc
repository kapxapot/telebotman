---
description: Always use pnpm as the package manager
alwaysApply: true
---

# Use pnpm

This project uses **pnpm**. Never use npm or yarn.

- `pnpm install` to install dependencies
- `pnpm add <pkg>` to add a package
- `pnpm remove <pkg>` to remove a package
- `pnpm run <script>` to run scripts
